# Reserved for future mission upload support (see
# HIL_MAVSDK_PYMAVLINK_AUTOMATED_DEBUG_PLAN.md section 7, "Send or upload mission
# waypoints"). Not read by main.py yet -- config.yaml's vehicle.mission_file just
# points here so the path exists once upload support is added.
waypoints: []
